import { TetrahedronGeometry } from "three";

const radius = 7;  // ui: radius
const detail = 0;  // ui: detail
const myTetrahedron = new TetrahedronGeometry(radius, detail);

export default myTetrahedron;